# Handles product data

product_object = {
    "width" : num,
    "height" : num,
    "length" : num,
    "weight": num,
    "color": string,
    "brand" : string
}

# Placeholder product list
product_list = []

